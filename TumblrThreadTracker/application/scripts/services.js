'use strict';

/* Services */


// Demonstrate how to register services
// In this case it is a simple value service.
angular.module('rpThreadTracker.services', [])
    .service('threadService', [function() {

        function getRecentThreads(count, userId) {
            
        }

    return {

    };
}])
